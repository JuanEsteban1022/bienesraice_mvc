div(class="bg-white shadow rounded-md")
    img(class="object-cover h-72 w-full" src=`/uploads/${propiedad.imagen}` alt=`Imagen casa ${propiedad.title}`)
    div(class="p-5 space-y-3")
        h3(class="text-2xl font-bold")= propiedad.title
        p(class="text-sm text-gray-600") Habitaciones: 
            span(class="text-gray-800 font-bold")= propiedad.habitaciones
        p(class="text-sm text-gray-600") Baños: 
            span(class="text-gray-800 font-bold")= propiedad.banos
        p(class="text-sm text-gray-600") Precio: 
            span(class="text-gray-800 font-bold")= propiedad.precio.name
        a(href=`/properties/${propiedad.id}` class="bg-blue-600 w-full text-center block font-bold text-white p-2 uppercase rounded") Ver Propiedad